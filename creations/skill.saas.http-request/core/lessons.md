# Lessons — http-request

No operational lessons yet (hand-seeded 2026-07-07). The auth-header rejection eval encodes
the credential rule at the sharpest edge: a literal token in `headers` must fail, not work.
"Repeated raw calls ⇒ generate a dedicated skill" is this library's growth signal.
