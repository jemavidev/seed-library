# Lessons — message-dispatcher

No operational lessons yet (hand-seeded 2026-07-07). Configured-destinations-only is the
anti-exfiltration property: an agent that can message arbitrary addresses is an agent that
can leak to arbitrary addresses.
