# Lessons — container-operator

No operational lessons yet (hand-seeded 2026-07-07). Immutable tags + never-overwrite-a-push
is the registry hygiene rule that keeps rollbacks meaningful; logs-before-restart preserves
the evidence agents otherwise destroy reflexively.
