# Lessons — agent.integrations-operator

No operational lessons yet (hand-seeded 2026-07-07). Undo-handles-recorded-as-you-go is the
crash-safety property: a flow that dies mid-way leaves a sweepable trail instead of orphaned
external artifacts.
