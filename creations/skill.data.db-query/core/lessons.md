# Lessons — db-query

No operational lessons yet (hand-seeded 2026-07-07). The read/write split into two skills is
the effect-class discipline applied to SQL: one interface that "usually reads" is one approval
policy that can never be strict enough.
