# Lessons — run-test-suite

No operational lessons yet (hand-seeded 2026-07-07). The "red suite is a result, not a tool
failure" distinction is the load-bearing design decision: it is what lets reflection loops
consume this tool as their evaluator without retry storms. Related: lesson
`cyclic-patterns-declare-termination`.
