# Lessons — db-write

No operational lessons yet (hand-seeded 2026-07-07). The mandatory inverse statement makes
reversibility a *precondition* of the effect instead of an afterthought; `expected_max_rows`
turns the classic missing-WHERE catastrophe into a typed, rolled-back failure.
