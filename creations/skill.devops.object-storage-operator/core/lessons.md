# Lessons — object-storage-operator

No operational lessons yet (hand-seeded 2026-07-07). `delete_unversioned_blocked` is the
effects rule inverted into a precondition: instead of declaring the delete uncompensable,
the skill refuses to perform deletes whose compensation cannot exist.
